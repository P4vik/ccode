{
  "name": "users-api",
  "version": "1.0.0",
  "description": "REST API demo — Node.js/Express z in-memory storage",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --forceExit --verbose"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4",
    "typescript": "^6.0.2"
  }
}
